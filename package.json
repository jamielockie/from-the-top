{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "publish": "lerna run build && lerna publish",
    "build": "lerna run build"
  }
}
